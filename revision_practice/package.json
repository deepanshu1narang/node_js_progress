{
  "name": "revision_practice",
  "version": "1.0.0",
  "description": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --watch index",
    "start:server": "node --watch server",
    "start:serverWithExpress": "node --watch serverWithExpress"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
